package driver;

import abstractinterp.scalar.state.ZoneState;

public class ZoneAnalysisRunner extends AnalysisRunner<ZoneState> {
    public ZoneAnalysisRunner(AnalysisOptions options) {
        super(options);
    }
}
