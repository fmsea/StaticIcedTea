package driver;

import abstractinterp.scalar.state.DeferredIncrementalOctagonState;

public class DeferredIncrementalOctagonAnalysisRunner extends AnalysisRunner<DeferredIncrementalOctagonState> {

    public DeferredIncrementalOctagonAnalysisRunner(AnalysisOptions options) {
        super(options);
    }
}
