package driver;

import abstractinterp.scalar.state.MaxZoneState;

public class MaxAnalysisRunner extends AnalysisRunner<MaxZoneState> {
    public MaxAnalysisRunner(AnalysisOptions options) {
        super(options);
    }
}
