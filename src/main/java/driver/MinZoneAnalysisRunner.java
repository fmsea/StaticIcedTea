package driver;

import abstractinterp.scalar.state.MinZoneState;

public class MinZoneAnalysisRunner extends AnalysisRunner<MinZoneState> {
    public MinZoneAnalysisRunner(AnalysisOptions options) {
        super(options);
    }
}
